import React from "react";
import { Link } from "react-router-dom";
import Nav from "../components/Nav";

const Main: React.FC = () => {
    return (
        <div>
            <Nav/>
            <h1>Main page</h1>
        </div>
    );
}

export default Main;